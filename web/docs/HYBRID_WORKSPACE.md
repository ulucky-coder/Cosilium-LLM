# LLM-top Hybrid Workspace

## Обзор системы

**LLM-top** — мульти-агентная аналитическая система, объединяющая 4 LLM-модели (ChatGPT, Claude, Gemini, DeepSeek) для экспертного анализа задач с разных точек зрения.

**Hybrid Workspace** — адаптивный UI с 4 режимами просмотра, оптимизированными под разные сценарии использования.

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────────────┐
│                              page.tsx                                │
├─────────────────────────────────────────────────────────────────────┤
│  Header.tsx                                                          │
│  ├── Logo + Sidebar Toggle                                          │
│  ├── ModeSwitcher (4 кнопки режимов)                                │
│  ├── Command Bar (⌘K поиск)                                         │
│  └── Agent Status Indicators (4 цветных точки)                      │
├─────────────────────────────────────────────────────────────────────┤
│  WorkspaceShell                                                      │
│  ├── Sidebar (скрывается в Present)                                 │
│  ├── ModeRouter → { Workspace | Document | Present | Compare }      │
│  └── Drawer (инспектор агентов, скрывается в Present)               │
├─────────────────────────────────────────────────────────────────────┤
│  StatusBar.tsx (скрывается в Present)                               │
├─────────────────────────────────────────────────────────────────────┤
│  CommandPalette.tsx (модальное окно)                                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Технологический стек

| Слой | Технология | Версия |
|------|------------|--------|
| Framework | Next.js App Router | 16.1.6 |
| UI Library | Radix UI Primitives | 1.x |
| State | Zustand | 5.0.11 |
| Styling | Tailwind CSS v4 | 4.x |
| Icons | Lucide React | 0.563.0 |
| Fonts | Inter + JetBrains Mono | - |

---

## State Management

### sessionStore.ts (421 строк)

Управляет данными сессий анализа:

```typescript
interface Session {
  id: string;                    // UUID сессии
  status: "input" | "running" | "paused" | "complete" | "error";
  task: string;                  // Текст задачи
  taskType: TaskType;            // strategy | research | investment | development | audit
  settings: SessionSettings;     // Настройки анализа
  currentIteration: number;      // Текущая итерация
  timeline: TimelineEvent[];     // Хронология событий
  analyses: AgentAnalysis[];     // Ответы агентов
  critiques: CritiqueEntry[];    // Критика между агентами
  synthesis: SynthesisResult;    // Объединённый результат
  metrics: {
    consensus: number;           // Уровень согласия (0-1)
    totalTokens: number;
    totalCost: number;
    duration: number;
  };
  notes: Note[];                 // Заметки пользователя
  tags: string[];                // Теги
  createdAt: Date;
  updatedAt: Date;
}
```

**Ключевые actions:**
- `createSession()` — создать новую сессию
- `startAnalysis()` — запустить анализ
- `addAnalysis(analysis)` — добавить ответ агента
- `setSynthesis(synthesis)` — установить синтез
- `loadSession(id)` — загрузить сессию из истории

### uiStore.ts (210 строк)

Управляет состоянием интерфейса:

```typescript
interface UIState {
  // Режим просмотра
  viewMode: "workspace" | "document" | "present" | "compare";
  previousViewMode: ViewMode;

  // Layout
  sidebarCollapsed: boolean;
  drawerOpen: boolean;
  drawerContent: "agent" | "critique" | "settings" | "export" | null;

  // Present mode
  presentSlideIndex: number;
  speakerNotesVisible: boolean;
  presentFullscreen: boolean;

  // Compare mode
  compareSessionIds: [string | null, string | null];
  compareSyncScroll: boolean;
  compareShowDiff: boolean;

  // Command palette
  commandPaletteOpen: boolean;
  commandQuery: string;

  // Sections (Document mode)
  expandedSections: Set<string>;
}
```

---

## 4 Режима просмотра

### 1. Workspace Mode (Рабочая область)

**Назначение:** Максимальная информационная плотность для активной работы.

**Layout:**
```
┌───────────┬─────────────────────────────────┬────────────┐
│ Sessions  │         Analysis Panel          │   Drawer   │
│ Panel     │  ┌─────────────────────────┐    │  (Agent    │
│           │  │ Input Form              │    │  Inspector)│
│ - Filters │  │ - Task textarea         │    │            │
│ - History │  │ - Task type selector    │    │            │
│ - Tags    │  │ - Iterations/Budget     │    │            │
│           │  │ - Advanced settings     │    │            │
│           │  └─────────────────────────┘    │            │
│           │                                  │            │
│           │  ┌─────────────────────────┐    │            │
│           │  │ Agent Cards Grid (2x2)  │    │            │
│           │  └─────────────────────────┘    │            │
│           │                                  │            │
│           │  ┌─────────────────────────┐    │            │
│           │  │ Tabs: Synthesis/Concl.  │    │            │
│           │  └─────────────────────────┘    │            │
└───────────┴─────────────────────────────────┴────────────┘
```

**Компоненты:**
- `WorkspaceLayout.tsx` — основной layout
- `SessionsPanel.tsx` — левая панель с историей сессий
- `AnalysisPanel.tsx` — центральная панель (InputPanel + ResultsPanel)
- `AgentCardsGrid.tsx` — сетка карточек агентов

**Функции:**
- Создание новой сессии (`N`)
- Ввод задачи и настройка параметров
- Запуск мульти-агентного анализа (`⌘Enter`)
- Просмотр результатов по агентам
- Инспектирование отдельного агента в Drawer

---

### 2. Document Mode (Документ)

**Назначение:** Полноэкранный режим чтения и экспорта, подобный Notion.

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│  [Export: MD ▼] [PDF] [JSON] [Copy]                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  # Анализ: Market Entry Strategy                        │
│  ───────────────────────────────────────────────────── │
│  Session #abc123 | Strategy | 87% Consensus             │
│                                                         │
│  ▼ Ввод                                                 │
│    [Текст задачи...]                                    │
│                                                         │
│  ▼ Анализы агентов (4)                                  │
│    ┌─ ChatGPT (92% уверенность)                        │
│    │  [Анализ...]                                       │
│    │  Ключевые моменты: • ... • ...                    │
│    │  ⚠ Риски: • ...                                   │
│    ├─ Claude (88% уверенность)                         │
│    │  ...                                               │
│                                                         │
│  ▼ Синтез                                               │
│    [Объединённый анализ...]                            │
│                                                         │
│  ▼ Выводы (5)                                           │
│    1. [Вывод] — 85% вероятность                        │
│    2. ...                                               │
│                                                         │
│  ▼ Рекомендации (3)                                     │
│    ⭐ Option A (9/10) — [описание]                      │
│       + pros  - cons                                    │
│                                                         │
│  ───────────────────────────────────────────────────── │
│  Создано в LLM-top | Session: abc123                    │
└─────────────────────────────────────────────────────────┘
```

**Компоненты:**
- `DocumentLayout.tsx` — prose layout
- `CollapsibleSection.tsx` — раскрывающиеся секции
- `ExportToolbar.tsx` — панель экспорта

**Функции:**
- Полноширинный контент для чтения
- Раскрывающиеся секции (`⌘↑`/`⌘↓` — свернуть/развернуть все)
- Экспорт в MD, JSON, PDF, CSV
- Print-ready форматирование

---

### 3. Present Mode (Презентация)

**Назначение:** Полноэкранная презентация результатов анализа.

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│                              [Notes] [Fullscreen] [X]   │
│                                                         │
│                                                         │
│                                                         │
│                    ┌───────────────────┐                │
│                    │                   │                │
│                    │   SLIDE CONTENT   │                │
│                    │                   │                │
│                    │   Large Title     │                │
│                    │   • Bullet point  │                │
│                    │   • Bullet point  │                │
│                    │                   │                │
│                    └───────────────────┘                │
│                                                         │
│                                                         │
│                                                         │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  [←]  ● ● ● ◉ ● ● ●  [→]              3 / 8            │
└─────────────────────────────────────────────────────────┘

┌─ Speaker Notes (N) ─────────────────────────────────────┐
│ Уверенность агента: 92%. Время: 3.2с. Токенов: 1,234   │
└─────────────────────────────────────────────────────────┘
```

**Слайды генерируются автоматически:**

| # | Тип | Содержимое |
|---|-----|------------|
| 1 | title | Название задачи + тип анализа |
| 2 | input | Полный текст задачи |
| 3-6 | agent | По 1 слайду на агента (ключевые моменты, риски) |
| 7 | consensus | Матрица согласия между агентами |
| 8 | synthesis | Объединённый анализ |
| 9+ | conclusion | Выводы (группами по 3) |
| N | recommendation | Рекомендации с оценками |

**Компоненты:**
- `PresentLayout.tsx` — fullscreen container
- `SlideView.tsx` — рендер слайда по типу
- `SlideNavigation.tsx` — точки + стрелки навигации
- `SpeakerNotes.tsx` — заметки докладчика
- `useSlideBuilder.ts` — генерация слайдов из сессии

**Управление:**
| Клавиша | Действие |
|---------|----------|
| `Space` / `→` / `j` | Следующий слайд |
| `←` / `k` | Предыдущий слайд |
| `f` | Полноэкранный режим |
| `n` | Показать/скрыть заметки |
| `Esc` | Выйти из презентации |
| Swipe (touch) | Навигация на мобильных |

---

### 4. Compare Mode (Сравнение)

**Назначение:** Side-by-side сравнение двух сессий анализа.

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│  [Session A ▼]              [Session B ▼]               │
│                                   [Sync] [Diff]         │
├────────────────────────┬────────────────────────────────┤
│  Diff Summary: Consensus A: 87% vs B: 72% (↓15%)       │
├────────────────────────┼────────────────────────────────┤
│  A  #abc123  87%       │  B  #def456  72%              │
├────────────────────────┼────────────────────────────────┤
│  Ввод                  │  Ввод                          │
│  [Текст задачи A]      │  [Текст задачи B]              │
├────────────────────────┼────────────────────────────────┤
│  Уверенность агентов   │  Уверенность агентов           │
│  ███████░░ GPT 92%     │  █████░░░░ GPT 78%    ↓14%    │
│  ██████░░░ Claude 88%  │  ███████░░ Claude 91%  ↑3%    │
│  ████████░ Gemini 95%  │  █████░░░░ Gemini 72%  ↓23%   │
│  ███████░░ DeepSeek 90%│  ██████░░░ DeepSeek 85% ↓5%   │
├────────────────────────┼────────────────────────────────┤
│  Синтез                │  Синтез                        │
│  [Summary A]           │  [Summary B]                   │
├────────────────────────┼────────────────────────────────┤
│  Выводы (4)            │  Выводы (3)                    │
│  1. ...                │  1. ...                        │
│  2. ...                │  2. ...                        │
├────────────────────────┼────────────────────────────────┤
│  Метрики               │  Метрики                       │
│  Токены: 4,521         │  Токены: 3,892                 │
│  Стоимость: $0.45      │  Стоимость: $0.38              │
└────────────────────────┴────────────────────────────────┘
```

**Компоненты:**
- `CompareLayout.tsx` — side-by-side layout
- `SessionSelector.tsx` — выбор сессий для сравнения
- `SyncedScrollArea.tsx` — синхронизированный скролл
- `DiffHighlighter.tsx` — подсветка различий
- `AgentConfidenceComparison.tsx` — сравнение уверенности

**Функции:**
- Выбор двух сессий из истории
- Синхронный скролл (`S`)
- Подсветка различий (`D`)
- Сравнение по метрикам: консенсус, токены, стоимость
- Визуальные индикаторы изменений (↑↓)

---

## Keyboard Shortcuts

### Глобальные

| Клавиша | Действие |
|---------|----------|
| `⌘K` | Командная палитра |
| `1` | Workspace mode |
| `2` | Document mode |
| `3` | Present mode |
| `4` | Compare mode |
| `m` | Циклический переход между режимами |
| `n` | Новая сессия |
| `/` | Поиск |
| `⌘\` | Toggle sidebar |
| `⌘Shift\` | Toggle drawer |

### По режимам

**Workspace:**
| Клавиша | Действие |
|---------|----------|
| `⌘Enter` | Запустить анализ |
| `⌘.` | Пауза анализа |

**Document:**
| Клавиша | Действие |
|---------|----------|
| `⌘↑` | Свернуть все секции |
| `⌘↓` | Развернуть все секции |

**Present:**
| Клавиша | Действие |
|---------|----------|
| `Space`/`→`/`j` | Следующий слайд |
| `←`/`k` | Предыдущий слайд |
| `f` | Fullscreen |
| `n` | Speaker notes |
| `Esc` | Выход |

**Compare:**
| Клавиша | Действие |
|---------|----------|
| `s` | Toggle sync scroll |
| `d` | Toggle diff highlight |

---

## API Integration

**Endpoint:** `POST /analyze`

**Request:**
```json
{
  "task": "Проанализировать рынок...",
  "task_type": "strategy",
  "max_iterations": 3
}
```

**Response:**
```json
{
  "analyses": [
    {
      "agent_name": "ChatGPT",
      "confidence": 0.92,
      "analysis": "...",
      "key_points": ["...", "..."],
      "risks": ["..."],
      "assumptions": ["..."],
      "duration": 3.2,
      "tokens": 1234,
      "cost": 0.12
    }
  ],
  "synthesis": {
    "summary": "...",
    "conclusions": [
      {
        "conclusion": "...",
        "probability": "85%",
        "falsification_condition": "..."
      }
    ],
    "recommendations": [
      {
        "option": "Option A",
        "description": "...",
        "score": 9,
        "pros": ["..."],
        "cons": ["..."]
      }
    ],
    "consensus_level": 0.87
  }
}
```

---

## Mobile Adaptations

| Режим | Адаптация |
|-------|-----------|
| **Workspace** | Sessions Panel скрыта, Drawer как модал |
| **Document** | Полная ширина, FAB для экспорта |
| **Present** | Swipe-навигация, tap по краям |
| **Compare** | Вертикальный стек вместо колонок |

---

## Файловая структура

```
src/
├── app/
│   ├── page.tsx                 # Entry point + ModeRouter
│   ├── layout.tsx               # Root layout
│   └── globals.css              # Tailwind config
├── components/
│   ├── mode/
│   │   ├── ModeSwitcher.tsx     # Переключатель режимов
│   │   └── ModeRouter.tsx       # Роутер с lazy loading
│   ├── workspace/
│   │   ├── WorkspaceLayout.tsx
│   │   ├── SessionsPanel.tsx
│   │   ├── AnalysisPanel.tsx
│   │   └── AgentCardsGrid.tsx
│   ├── document/
│   │   ├── DocumentLayout.tsx
│   │   ├── CollapsibleSection.tsx
│   │   └── ExportToolbar.tsx
│   ├── present/
│   │   ├── PresentLayout.tsx
│   │   ├── SlideView.tsx
│   │   ├── SlideNavigation.tsx
│   │   ├── SpeakerNotes.tsx
│   │   └── useSlideBuilder.ts
│   ├── compare/
│   │   ├── CompareLayout.tsx
│   │   ├── SessionSelector.tsx
│   │   ├── SyncedScrollArea.tsx
│   │   └── DiffHighlighter.tsx
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── Drawer.tsx
│   │   └── StatusBar.tsx
│   ├── command/
│   │   └── CommandPalette.tsx
│   └── ui/                      # Radix primitives
│       ├── button.tsx
│       ├── card.tsx
│       ├── tabs.tsx
│       └── ...
├── stores/
│   ├── sessionStore.ts          # Session state
│   └── uiStore.ts               # UI state
├── hooks/
│   └── useKeyboardShortcuts.ts  # Global shortcuts
└── lib/
    ├── constants.ts             # AGENTS, VIEW_MODES, SHORTCUTS
    └── utils.ts                 # cn() utility
```

---

## 4 Агента системы

| Агент | Роль | Цвет |
|-------|------|------|
| **ChatGPT** | Логический аналитик | 🟢 Emerald |
| **Claude** | Системный архитектор | 🟠 Amber |
| **Gemini** | Генератор альтернатив | 🔵 Blue |
| **DeepSeek** | Формальный аналитик | 🟣 Violet |

---

## Типы анализа

| Тип | Описание |
|-----|----------|
| **strategy** | Рынки, конкуренты, бизнес-решения |
| **research** | Глубокий анализ любой темы |
| **investment** | Оценка проектов, риски, ROI |
| **development** | Архитектура, технические решения |
| **audit** | Проверка методологии, поиск ошибок |

---

## Резюме

Hybrid Workspace предоставляет 4 оптимизированных режима для работы с мульти-агентным анализом:

1. **Workspace** — активная работа, максимум информации
2. **Document** — чтение и экспорт, минимум chrome
3. **Present** — демонстрация результатов
4. **Compare** — сравнение сессий

Переключение между режимами мгновенное (клавиши 1-4), все данные сохраняются в Zustand stores с персистенцией.
